<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/clipboard@2.0.10/dist/clipboard.min.js"></script>
    </head>
    <style>
        .align-vertical {
          display: flex;
          justify-content: center;
          align-items: center;
          text-align: center;
          min-height: 100vh;
        }
    </style>
    <body class="bg-dark">
        <div class="align-vertical">
            <div class="container">
                <div class="page-header">
                    <h3 class="text-danger text-center mt-2 mb-4">Surtains Epic Resolver</h3>
                </div>
                <center>
                    <div class="alert alert-success">
                        <strong>Success!</strong> We've located the account ;)
                    </div>
                    <div class="border border-light rounded-2 p-5">
                        <span class="text-light"><strong>Display Name: </strong><%= account.displayName %></span>
                        <br>
                        <span id="accountId" class="text-light"><strong>Account ID: </strong><%= account.id %></span>
                        <br>
                        <button id="copy-id" data-clipboard-target="#accountId" class="btn btn-dark btn-outline-light mt-3">Copy ID</button>
                        <a href="../"><button class="btn btn-dark btn-outline-light mt-3 mx-4">👈 Back</button></a>
                    </div>
                </center>
            </div>
        </div>
        <script type="text/javascript">
            var clipboard = new ClipboardJS('#copy-id');
            clipboard.on('success', function(e) {
            		e.clearSelection();
            		document.getElementById("copy-id").textContent = "Copied ✔";
            });
			clipboard.on('error', function(e) {
                e.clearSelection();
                document.getElementById("copy-id").textContent = "Error ❌";
            });
	    </script>
    </body>
</html>